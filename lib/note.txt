radius: MediaQuery.of(context).size.width / 4,


 Stack(
        alignment: Alignment.topCenter,
        children: [
          Column(
            mainAxisSize: MainAxisSize.min,
            children: [
              Container(
                width: size / 2,
                margin: EdgeInsets.only(top: (size / 2) / 2),
                padding: const EdgeInsets.all(AppPadding.p30),
                decoration: BoxDecoration(
                  color: ColorManager.whiteColor,
                  borderRadius: BorderRadius.circular(
                    12,
                  ),
                ),
                child: Column(
                  children: [
                    const SizedBox(
                      height: AppSize.s20,
                    ),
                    Text(
                      popularSingerModel.name,
                      style: StyleManage.boldTextStyle(),
                    ),
                    Text(
                      '${popularSingerModel.followers}, Followers',
                    ),
                  ],
                ),
              ),
            ],
          ),
          Hero(
            tag: popularSingerModel.image,
            child: CircleAvatar(
              backgroundImage: NetworkImage(popularSingerModel.image),
              radius: (size / 3) / 2,
            ),
          ),
        ],
      ),